aws_region            = "eu-central-1"         # или тот регион, куда деплоим
aws_credentials_file  = "~/.aws/credentials"   # только для локальной работы
aws_profile           = "tekero-dev"           # если в ~/.aws/credentials есть профиль
environment           = "dev"
project               = "tekero"
